[
  {
    "name": "Admin Full Access",
    "description": "Admin can do all.",
    "target": "subject.role.name == 'admin'",
    "condition": "true"
  },
  {
    "name": "Create new users",
    "description": "Admin and Manager can create new users.",
    "target": "action == 'CREATE_USER'",
    "condition": "{'admin', 'manager'}.contains(subject.role.name)"
  },
  {
    "name": "Filter roles",
    "description": "Managers can create new users with equal roles or lower permissions.",
    "target": "action == 'FILTER_ROLE'",
    "condition": "subject.role.name == 'admin' OR (subject.role.name == 'manager' AND {'manager', 'general'}.contains(resource.name))"
  },
  {
    "name": "TEST access plan",
    "description": "Test.",
    "target": "action == 'READ_PLAN'",
    "condition": "true"
  },
  {
    "name": "TEST access plan",
    "description": "Test.",
    "target": "action == 'FILTER_PLAN'",
    "condition": "subject.login != 'anonymousUser' OR resource.planPrivacy.name == 'PUBLIC'"
  },
  {
    "name": "...",
    "description": "...",
    "target": "action == 'VIEW_NON_PUBLIC_DATA'",
    "condition": "subject.login != 'anonymousUser'"
  },
  {
    "name": "Access own plans",
    "description": "User can see all but protected if not in his/her centre",
    "target": "subject.role.name == 'N0_ADMIN' AND action == 'PLAN_LIST'",
    "condition": "subject.productionCentre.id == resource.productionCentre.id or resource.privacy == 'public'"
  }
]